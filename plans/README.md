# Implementation Plans

Generated by the `improve` skill on 2026-07-13, audited against commit `c8d53b2`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Establish a golden-file verification baseline | P1 | M | — | DONE (worktree agent-a77a4d8, commit 54b5e09; not merged) |
| 002  | Harden the two Flask web panels | P1 | M | — | TODO |
| 003  | Fix price bugs + add price characterization tests | P1 | M | 001 (recommended) | TODO |
| 004  | Delete dead code and reconcile doc drift | P2 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Dependency notes

- **003 should follow 001.** Plan 003 changes price-calculation behavior
  (`step_pricing.py`) and price-selection behavior (`price_utils.py`). The
  golden-file harness from 001 is the safety net that proves the WooCommerce
  output did not regress. 003's *unit tests* for `price_utils` are standalone
  and can be written without 001, but do not merge the `step_pricing` change
  until 001's harness exists and passes on the current baseline.
- **002 and 004 are independent** — run them in any order, before or after the others.

## Findings considered and rejected / deferred

These were audited and consciously NOT turned into plans this round:

- **`product_extraction/web_panel_interactive.py:426` debug flag**: already
  `debug=False`. Only its `0.0.0.0` binding + traversal are real — folded into plan 002.
- **`.env` secret exposure**: the on-disk `.env` is correctly gitignored (only
  `.env.example` is tracked) and its keys are currently empty. No committed
  secret, no rotation needed today. Not a finding — keep it gitignored.
- **SKU regex over-capture** (`spec_scraper.py:652`) and **retry-count
  miscalculation** (`spec_scraper.py:1618`): real but MED confidence and lower
  leverage; revisit after the baseline harness exists to verify any change.
- **`import_builder` bypasses `common/`** (3 divergent color managers, trapped
  shared package): real L-effort architectural debt. Deferred — too large for
  one safe plan without the baseline harness first. Candidate for a future round.
- **Plaintext FTP + TLS-ignore** (`image_processor_v9_1_fixed.py`,
  `Image_Downloader.py:132`): real but credentials are entered interactively
  (not committed) and this is a local scraping tool. Lower leverage; deferred.
- **Truncated-image acceptance** (`Image_Downloader.py:322`) and **unescaped
  HTML report** (`price_tracker.py:709`): real MED/LOW items; deferred to keep
  this round focused on the selected clusters.
